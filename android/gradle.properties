# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# Adjusted to 2GB to leave room for Kotlin compiler and system processes
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8 -XX:+UseParallelGC

# CI Optimization: Disable daemon to prevent memory leaks/hangs
org.gradle.daemon=false

# Disable parallel execution to prevent race conditions and make logs clearer
org.gradle.parallel=false

# AndroidX package structure configuration
android.useAndroidX=true
android.enableJetifier=true
